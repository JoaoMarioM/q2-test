import styled from "styled-components/native";
import { responsiveHeight } from "../../utils/responsive";

export const Container = styled.View`
    margin-top: ${responsiveHeight(23)}px;
    margin-bottom: ${responsiveHeight(24)}px;
    width: 100%;
`